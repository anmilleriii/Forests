import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    primary: "darkslategray",
    secondary: "white",
    badges: {
      conservation: "green",
      reforestation: "telegram",
    },
  },
});
